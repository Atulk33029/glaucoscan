services:
  - type: web
    name: glaucoscan
    runtime: python
    buildCommand: "pip install --upgrade pip setuptools wheel && pip install flask flask-cors opencv-python-headless Pillow numpy reportlab werkzeug gunicorn requests torch torchvision"
    startCommand: "gunicorn app:app --timeout 120 --workers 1"
